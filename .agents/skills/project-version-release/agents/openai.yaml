agent:
  display_name: "Project Version Release"
  short_description: "维护 changeLog、版本及 tar.gz/zip GitHub Release 资产"
  default_prompt: "使用 project-version-release skill。先分类版本影响并 dry-run；GitHub Go 发布时读取 github-go-binary-release 参考，显式输入版本，通过 release-verify 验证 tar.gz、zip 和 SHA256SUMS 后再打 tag 和发布。"
